#!/usr/bin/env bash
# $3 is 1 for a branch checkout and 0 for a file checkout. Only the branch case can move
# the libraries out from under the deployed tree.
[ "${3:-0}" = 1 ] || exit 0
exec bash "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/kit-drift.sh"
